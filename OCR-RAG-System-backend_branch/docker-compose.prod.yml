services:
  ocr-rag-system:
    image: anil7777/ocr-rag-system:latest
    container_name: ocr-rag-system
    ports:
      - "8000:8000"
    volumes:
      # Mount application code for hot-reloading during development
      - ./app:/app/app
      # Mount main.py for hot-reloading
      - ./main.py:/app/main.py
      # Mount media and data directories for persistence
      - ./media:/app/media
      - ./data:/app/data
      # Mount config files (service account keys)
      - ./app/config/serviceAccountKey.json:/app/app/config/serviceAccountKey.json:ro
      - ./app/config/google_service_account.json:/app/app/config/google_service_account.json:ro
      - ./app/config/gemini-key.json:/app/app/config/gemini-key.json:ro
      # Mount counters.json for persistence
      - ./counters.json:/app/counters.json
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Kathmandu
    env_file:
      - .env
    command: [ "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload" ]
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/', timeout=5)" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
